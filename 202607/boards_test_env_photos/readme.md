说明：本目录存放测试环境的照片。在本测试目标下，存放RISC-V开发板在测试运行过程中的照片，用于在测试报告中表达测试用例执行的环境说明。

建议：
1. 在设备运行的状态下，拍摄开发板工作时的照片为最佳
2. 如果有屏幕，最好在将屏幕调整为显示设备硬件信息，如执行：

```bash
# 显示设备型号，强烈要求
cat /proc/device-tree/model   

# 显示操作系统信息
cat /etc/os-release  #（或者 uname -a）

# 显示cpuinfo
cat /proc/cpuinfo             

```

